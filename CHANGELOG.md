# บันทึกการอัปเดตระบบ (Changelog)

## [2026-08-25] - สรุปการแก้ไขล่าสุด

**1. ระบบข้อมูลตารางคะแนน (League Table)**
- **เพิ่มสคริปต์ Sync ข้อมูล:** สร้างไฟล์ `scripts/sync-table.mjs` สำหรับดึงข้อมูลตารางคะแนนพรีเมียร์ลีกโดยตรงจาก Google Sheets (`1mdFJwRXRB-xBYiDMJK0LoUD9n3Jf9iF1x6NH1V4W1gY`) และแปลงรูปแบบเป็น JSON อัตโนมัติ
- **อัปเดตข้อมูลล่าสุด:** อัปเดตข้อมูลตารางคะแนนทีมชายในไฟล์ `data/tables-men.json` ให้เป็นข้อมูลปัจจุบันตามที่ได้รับมอบหมายเรียบร้อยแล้ว
- **แก้ไขรูปแบบสไตล์ (UI):** ปรับแต่งสไตล์ (CSS) สีข้อความอันดับในตารางคะแนน (บรรทัดที่ 6) ให้เป็นสีเหลือง (`#ffd200`) และเป็นตัวหนาตามคำขอ

**2. ระบบโปรแกรมและผลการแข่งขัน (Fixtures & Results)**
- **อัปเดตโลโก้รายการแข่งขัน:** แก้ไขและอัปเดตข้อมูลในไฟล์ `data/fixtures.json` และ `data/results-men.json` โดยตั้งค่ารูปโลโก้พรีเมียร์ลีก (`databases/logo/competitions/men/premier-league.png`) เข้าไปในการแข่งขันพรีเมียร์ลีกทั้งหมด
- **แก้ไขบั๊กข้อมูล:** ลบเครื่องหมายจุลภาค (comma `,`) ที่เกินมาจากโครงสร้างอาร์เรย์ `goals` ในไฟล์ `data/fixtures.json` ทำให้แอปกลับมาโหลดข้อมูล JSON ได้อย่างถูกต้อง

**3. การแก้ไขข้อผิดพลาดระบบ (System Fixes)**
- **แก้ไขเซิร์ฟเวอร์:** แก้ไขปัญหาพอร์ตชนกัน (`EADDRINUSE`) บน Dev Server โดยกำหนดให้ `server.js` ใช้งานพอร์ต `3000` แบบคงที่ ทำให้แอปกลับมารันและทำงานได้ตามปกติ
